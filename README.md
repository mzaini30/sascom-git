# Clone Git

```bash
git clone --depth=1 https://username:password@github.com/username/repositori
```

# Upload Git

```bash
git status
git add -A .
git commit -m "upload"
git push
```

# Update Repositori Lokal

```bash
git pull
```
